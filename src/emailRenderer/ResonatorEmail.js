import React from 'react';
import oy from 'oy-vey';

export default ({resonator}) => {
    return (
        <div style={{background: 'red'}}>
            Resonator title: {resonator.title}
        </div>
    );
}
